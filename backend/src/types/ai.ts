/**
 * Khatabook AI — Backend AI Type Re-exports
 *
 * Re-exports all AI-specific shared types so backend code can import
 * from `@/types/ai` and get the canonical shared definitions.
 */

export * from '../../../shared/types/ai';
